export interface ExamInfo {
  exam: string;
  name: string;
  date: string;
  resultDate: string;
}

export interface ExamCalendarEntry {
  exam: string;
  name: string;
  date: string;
  resultDate: string;
  icon: string;
  color: string;
}
